// Reference stub — not a working implementation.
//
// Owns reading a generated `.ts` file off disk as the "observed" side of
// conformance evaluation (Layer 21's first step). See
// Canonical Feature Authority File-System Spine.md, Layer 20-21.

export async function observesGeneratedBody(path: string): Promise<string> {
  throw new Error(
    `observesGeneratedBody is a documentation stub — see Canonical Feature Authority File-System Spine.md, Layer 20-21 (requested: ${path})`
  );
}
