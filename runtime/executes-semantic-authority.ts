// Reference stub — not a working implementation.
//
// Owns running the resolved observation → evaluation → disposition chain
// for one semantic edge against one context, producing the disposition
// (e.g. SCENARIO_ATOMIC / SCENARIO_NOT_ATOMIC / SCENARIO_ATOMICITY_UNRESOLVED)
// that resolves-scenario-atomicity-disposition.sej.json declares.

export function executesSemanticAuthority() {
  throw new Error(
    "executesSemanticAuthority is a documentation stub — see architecture/defines-authority-projection-boundary.md"
  );
}
