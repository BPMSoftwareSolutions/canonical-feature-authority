// Reference stub — not a working implementation.
//
// Owns deriving a `projects-typescript-ast.json` file (Layer 17) — the
// exact permitted syntax tree, plus its forbidden node list — from a
// `projects-typescript-body.json` declarative projection (Layer 16). See
// Canonical Feature Authority File-System Spine.md, Layer 17.

export function projectsTypescriptAst() {
  throw new Error(
    "projectsTypescriptAst is a documentation stub — see Canonical Feature Authority File-System Spine.md, Layer 17"
  );
}
