// Reference stub — not a working implementation.
//
// Owns the `cfa scaffold authority --scenario <scenarioId>` command:
// generating the obligation, scenario expectation, responsibility, signal,
// and semantic-authority record set (Spine Layers 5-13) for a new
// scenario folder. See The Student Experience.md, Section 12.

export async function projectsScenarioAuthority(scenarioId: string): Promise<void> {
  throw new Error(
    `projectsScenarioAuthority is a documentation stub — see The Student Experience.md, Section 12 (requested: ${scenarioId})`
  );
}
